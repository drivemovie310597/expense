unutuk mengaktifkan virtula env :
source venv/Scripts/activate


untuk mengetik di VIM tekan 'i'

linter(git pre-commit) : untuk memvalidasi kode kita sebelum melakukan push

menggunakan pre-commit :
git add .
git commit
git git push


cara menghapus cached pada git
git rm -r --cached -f .idea


TDD : melakukan test sebelum membuat code program
